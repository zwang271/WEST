INPUT
    b0,b1,b2: bool;

FTSPEC
    (G[0,5] b0) && (G[2,3] b0);
    (G[2,3] b0) && (G[0,5] b0);
    (G[0,5] b0) && (G[2,8] b0);
    (G[2,8] b0) && (G[0,5] b0);

    (G[0,5] b0) && (G[2,10] b1);
    (G[2,4] b0) && (G[1,4] b1);
    