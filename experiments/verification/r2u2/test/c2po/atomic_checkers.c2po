INPUT
    b: bool;
    i: int;
    f,r,ad,m: float;

ATOMIC
    a0 := b == true;
    a1 := i == 1;
    a2 := f > 1.0;
    --a3 := rate(r) > 1.0;
    --a4 := movavg(m,3) > 1.0;
    --a5 := abs_diff_angle(ad,50.0) < 120.0;

FTSPEC
    a0;
    a1;
    a2;
    --a3;
    --a4;
    --a5;