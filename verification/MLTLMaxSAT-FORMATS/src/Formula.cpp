#include <Formula.h>
#include <memory>
using namespace std;
// shared_ptr<Formula> operator&&(const Formula& a, const Formula& b){
    // shared_ptr<Formula> out(new Formula);
    
// };
std::string Formula::to_string(){
    return std::string("a");
};

Formula::Formula(/* args */) {}

Formula::~Formula() {}